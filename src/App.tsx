import React from 'react';\nimport { WizardPage } from './ui/pages/WizardPage';\nimport { ResultsPage } from './ui/pages/ResultsPage';\nimport { useDesignStore } from './ui/stores/designStore';\nimport './ui/styles/index.css';\n\ntype AppView = 'wizard' | 'results';\n\nfunction App() {\n  const [view, setView] = React.useState<AppView>('wizard');\n  const currentDesign = useDesignStore((state) => state.currentDesign);\n\n  React.useEffect(() => {\n    if (currentDesign) {\n      setView('results');\n    }\n  }, [currentDesign]);\n\n  return (\n    <div className=\"app\">\n      {/* Header */}\n      <header className=\"app-header\">\n        <div className=\"app-logo\">\n          <h1>☀️ KELEN SOLAR</h1>\n          <p>Dimensionnement intelligent d'installations solaires</p>\n        </div>\n        <nav className=\"app-nav\">\n          <button\n            onClick={() => setView('wizard')}\n            className={`nav-btn ${view === 'wizard' ? 'active' : ''}`}\n          >\n            Wizard\n          </button>\n          <button\n            onClick={() => setView('results')}\n            className={`nav-btn ${view === 'results' ? 'active' : ''}`}\n            disabled={!currentDesign}\n          >\n            Résultats\n          </button>\n        </nav>\n      </header>\n\n      {/* Main */}\n      <main className=\"app-main\">\n        {view === 'wizard' && <WizardPage />}\n        {view === 'results' && <ResultsPage />}\n      </main>\n\n      {/* Footer */}\n      <footer className=\"app-footer\">\n        <p>Built with ❤️ for solar professionals • v2.0 • TypeScript + React</p>\n      </footer>\n    </div>\n  );\n}\n\nexport default App;\n